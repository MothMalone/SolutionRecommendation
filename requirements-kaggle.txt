# Match AutoGluon 1.5's supported ABI range and keep Kaggle's NumPy 2 build.
# Pinning NumPy <2 downgrades it underneath Kaggle's prebuilt pandas and causes
# "numpy.dtype size changed" until the kernel is restarted.
numpy>=1.25,<2.4
pandas>=2,<2.4
scipy
requests
pyarrow
openml
# Install only Tabular since the project uses autogluon.tabular APIs.
# Let pip resolve compatible scikit-learn/xgboost versions with AutoGluon.
autogluon.tabular==1.5.0
